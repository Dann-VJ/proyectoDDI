import { StyleSheet } from "react-native";

export const styles2 = StyleSheet.create({
    title: {
        fontSize: 20,
        fontWeight: 'bold',
        color: '#fff',
        marginVertical: 10,
        textAlign: 'center'
    },
    sniper: {
        marginTop: 10,
        marginBottom: 50
    },
});